/**
 * Root entry point — `node server.js` or `npm start`.
 * Plain JavaScript now, so this just runs the app directly — no build
 * step, no compiler, no ts-node fallback needed.
 */
require("./src/app.js");
